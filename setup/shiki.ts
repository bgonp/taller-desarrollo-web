import { defineShikiSetup } from '@slidev/types'

export default defineShikiSetup(() => {
  return {
    theme: {
      dark: 'dark-plus',
      light: 'dark-plus',
      // dark: 'one-dark-pro',
      // light: 'one-dark-pro',
    },
  }
})